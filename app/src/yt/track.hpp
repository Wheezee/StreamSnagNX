#pragma once

#include <string>

namespace ssnx
{

struct Track
{
    std::string title;
    std::string artist;
    std::string subtitle;
    std::string video_id;
    std::string thumb_url;
};

} // namespace ssnx
